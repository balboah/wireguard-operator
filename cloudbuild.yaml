steps:
- name: 'gcr.io/cloud-builders/docker'
  id: 'build-operator'
  args: [
    'build',
    '-f', 'Dockerfile.operator',
    '-t', 'eu.gcr.io/$PROJECT_ID/wireguard-operator:latest',
    '.'
  ]
- name: 'gcr.io/cloud-builders/docker'
  id: 'build-wireguard'
  args: [
    'build',
    '-f', 'Dockerfile.wireguard',
    '-t', 'eu.gcr.io/$PROJECT_ID/wireguard:latest',
    '.'
  ]
  waitFor: '-'
images:
  - 'eu.gcr.io/$PROJECT_ID/wireguard-operator:latest'
  - 'eu.gcr.io/$PROJECT_ID/wireguard:latest'
